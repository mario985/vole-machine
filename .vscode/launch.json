{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug with CodeLLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/outputFile",  // Path to your executable
            "args": [],
            // "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
